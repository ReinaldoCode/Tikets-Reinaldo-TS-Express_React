export type Row = {
  type: string;
  name: string;
  lableText: string;
  defaultValue: string;
};

export type RowSelect = {
  name: string;
  lableText: string;
  defaultValue: string;
  list: string[];
};
